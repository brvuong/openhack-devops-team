name: .NET Core

on:
  push:    
    paths: 
      - '/apis/**'
    branches: [ master ]
      
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    
    - name: Run a multi-line script
      shell: bash
      run: echo "test"  

